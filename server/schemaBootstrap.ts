export const APPLICATION_SCHEMA_SQL = [
  `CREATE TABLE IF NOT EXISTS audit_logs (id int AUTO_INCREMENT NOT NULL, actorId int NOT NULL, serverId int, action varchar(100) NOT NULL, target varchar(255), metadata text, createdAt timestamp NOT NULL DEFAULT (now()), CONSTRAINT audit_logs_id PRIMARY KEY(id))`,
  `CREATE TABLE IF NOT EXISTS catalog_installations (id int AUTO_INCREMENT NOT NULL, serverId int NOT NULL, ownerId int NOT NULL, catalogType enum('modpack','plugin','map') NOT NULL, name varchar(160) NOT NULL, version varchar(64) NOT NULL, status enum('queued','installed','failed') NOT NULL DEFAULT 'queued', createdAt timestamp NOT NULL DEFAULT (now()), CONSTRAINT catalog_installations_id PRIMARY KEY(id))`,
  `CREATE TABLE IF NOT EXISTS minecraft_servers (id int AUTO_INCREMENT NOT NULL, ownerId int NOT NULL, name varchar(120) NOT NULL, slug varchar(140) NOT NULL, serverType enum('java','bedrock') NOT NULL DEFAULT 'java', core varchar(64) NOT NULL DEFAULT 'Paper', version varchar(32) NOT NULL DEFAULT '1.21.1', status enum('online','offline','starting','stopping') NOT NULL DEFAULT 'offline', maxPlayers int NOT NULL DEFAULT 20, playersOnline int NOT NULL DEFAULT 0, tps int NOT NULL DEFAULT 20, ramUsedMb int NOT NULL DEFAULT 0, ramTotalMb int NOT NULL DEFAULT 4096, cpuPercent int NOT NULL DEFAULT 0, diskUsedGb int NOT NULL DEFAULT 0, diskTotalGb int NOT NULL DEFAULT 40, address varchar(180), motd varchar(255), pvp int NOT NULL DEFAULT 1, onlineMode int NOT NULL DEFAULT 1, createdAt timestamp NOT NULL DEFAULT (now()), updatedAt timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP, CONSTRAINT minecraft_servers_id PRIMARY KEY(id))`,
  `CREATE TABLE IF NOT EXISTS server_actions (id int AUTO_INCREMENT NOT NULL, serverId int NOT NULL, ownerId int NOT NULL, action varchar(64) NOT NULL, payload text, output text, createdAt timestamp NOT NULL DEFAULT (now()), CONSTRAINT server_actions_id PRIMARY KEY(id))`,
  `CREATE TABLE IF NOT EXISTS server_backups (id int AUTO_INCREMENT NOT NULL, serverId int NOT NULL, ownerId int NOT NULL, name varchar(160) NOT NULL, sizeGb int NOT NULL DEFAULT 0, status enum('ready','creating','restoring','failed') NOT NULL DEFAULT 'ready', artifactStatus enum('idle','creating','ready','failed') NOT NULL DEFAULT 'idle', artifactKey varchar(255), createdAt timestamp NOT NULL DEFAULT (now()), CONSTRAINT server_backups_id PRIMARY KEY(id))`,
  `CREATE TABLE IF NOT EXISTS server_files (id int AUTO_INCREMENT NOT NULL, serverId int NOT NULL, ownerId int NOT NULL, path varchar(255) NOT NULL, name varchar(120) NOT NULL, kind enum('file','folder') NOT NULL DEFAULT 'file', sizeBytes int NOT NULL DEFAULT 0, storageKey varchar(255), createdAt timestamp NOT NULL DEFAULT (now()), updatedAt timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP, CONSTRAINT server_files_id PRIMARY KEY(id))`,
  `CREATE TABLE IF NOT EXISTS server_invitations (id int AUTO_INCREMENT NOT NULL, serverId int NOT NULL, inviterId int NOT NULL, email varchar(320) NOT NULL, role enum('admin','operator','viewer') NOT NULL DEFAULT 'viewer', tokenHash varchar(128) NOT NULL, expiresAt timestamp NOT NULL, acceptedAt timestamp, createdAt timestamp NOT NULL DEFAULT (now()), CONSTRAINT server_invitations_id PRIMARY KEY(id), CONSTRAINT server_invitations_tokenHash_unique UNIQUE(tokenHash))`,
  `CREATE TABLE IF NOT EXISTS server_logs (id int AUTO_INCREMENT NOT NULL, serverId int NOT NULL, ownerId int NOT NULL, level enum('system','info','warn','error','debug') NOT NULL DEFAULT 'info', source varchar(64) NOT NULL DEFAULT 'minecraft', message text NOT NULL, createdAt timestamp NOT NULL DEFAULT (now()), CONSTRAINT server_logs_id PRIMARY KEY(id))`,
  `CREATE TABLE IF NOT EXISTS server_members (id int AUTO_INCREMENT NOT NULL, serverId int NOT NULL, userId int NOT NULL, role enum('admin','operator','viewer') NOT NULL DEFAULT 'viewer', createdAt timestamp NOT NULL DEFAULT (now()), CONSTRAINT server_members_id PRIMARY KEY(id), CONSTRAINT server_members_server_user_unique UNIQUE(serverId,userId))`,
  `CREATE TABLE IF NOT EXISTS server_schedules (id int AUTO_INCREMENT NOT NULL, serverId int NOT NULL, ownerId int NOT NULL, name varchar(120) NOT NULL, cronExpression varchar(64) NOT NULL, action enum('restart') NOT NULL DEFAULT 'restart', taskUid varchar(65), enabled int NOT NULL DEFAULT 1, lastRunAt timestamp, createdAt timestamp NOT NULL DEFAULT (now()), updatedAt timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP, CONSTRAINT server_schedules_id PRIMARY KEY(id), CONSTRAINT server_schedules_task_uid_unique UNIQUE(taskUid))`,
  `CREATE TABLE IF NOT EXISTS server_webhooks (id int AUTO_INCREMENT NOT NULL, serverId int NOT NULL, ownerId int NOT NULL, externalHookId varchar(128) NOT NULL, secret varchar(255) NOT NULL, enabled int NOT NULL DEFAULT 1, lastEventId varchar(160), lastEventAt timestamp, createdAt timestamp NOT NULL DEFAULT (now()), updatedAt timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP, CONSTRAINT server_webhooks_id PRIMARY KEY(id), CONSTRAINT server_webhooks_external_unique UNIQUE(serverId,externalHookId))`,
  `CREATE TABLE IF NOT EXISTS users (id int AUTO_INCREMENT NOT NULL, openId varchar(64) NOT NULL, name text, email varchar(320), passwordHash varchar(255), discordId varchar(64), loginMethod varchar(64), role enum('user','admin') NOT NULL DEFAULT 'user', createdAt timestamp NOT NULL DEFAULT (now()), updatedAt timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP, lastSignedIn timestamp NOT NULL DEFAULT (now()), CONSTRAINT users_id PRIMARY KEY(id), CONSTRAINT users_openId_unique UNIQUE(openId), CONSTRAINT users_discord_id_unique UNIQUE(discordId))`,
  `CREATE TABLE IF NOT EXISTS webhook_events (id int AUTO_INCREMENT NOT NULL, webhookId int NOT NULL, eventKey varchar(190) NOT NULL, eventType varchar(100) NOT NULL, status enum('received','duplicate','failed') NOT NULL DEFAULT 'received', payload text NOT NULL, createdAt timestamp NOT NULL DEFAULT (now()), CONSTRAINT webhook_events_id PRIMARY KEY(id), CONSTRAINT webhook_events_key_unique UNIQUE(webhookId,eventKey))`,
  `CREATE INDEX IF NOT EXISTS audit_logs_actor_idx ON audit_logs (actorId)`,
  `CREATE INDEX IF NOT EXISTS audit_logs_server_idx ON audit_logs (serverId)`,
  `CREATE INDEX IF NOT EXISTS server_invitations_server_idx ON server_invitations (serverId)`,
  `CREATE INDEX IF NOT EXISTS server_members_user_idx ON server_members (userId)`,
  `CREATE INDEX IF NOT EXISTS server_schedules_owner_idx ON server_schedules (ownerId)`,
] as const;
